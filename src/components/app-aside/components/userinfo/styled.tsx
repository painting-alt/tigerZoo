import styled from 'styled-components'

import { Card } from 'antd'

export default styled(Card)`
    .upadteDataBtn {
        display: flex;
        align-items: center;

        .editIcon {
            margin: 0 10px;
        }
    }
`
